import 'package:flutter/material.dart';

class Selected_Image extends StatelessWidget {
  const Selected_Image({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}